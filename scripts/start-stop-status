#!/bin/sh

case $1 in
	start)
		exit 0
	;;
	stop)
		exit 0
	;;
	status)
		exit 0
	;;
	log)
		echo "App is installed properly"
		exit 0
	;;
	*)
        echo "Usage: $0 {start|stop|status}" >&2
        exit 1
	;;
esac
